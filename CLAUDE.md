# Claude Code Instructions

## UI Rules
- **DO NOT use HTML/CSS overlay for game UI**
- Use `three-mesh-ui` library for all in-game UI elements
- UI should be rendered as Three.js objects in the canvas, not as DOM elements
